In-place loading. Skeletons mirror the shape that is coming — including hexagons on the canvas. Never a full-screen blocking loader.

```jsx
<Skeleton variant="text" lines={3} />
<Skeleton variant="hex" width={64} height={55} />
<Spinner size={14} />
```
