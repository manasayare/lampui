Generic 20px label chip: semantic tones, memory accent, micro uppercase stamps, count chips.

```jsx
<Badge tone="memory" micro>Policy</Badge>
<Badge tone="warning" icon="triangle-alert">3 exceptions</Badge>
<Badge count tone="danger">7</Badge>
```

For runtime/operational state use StatusBadge instead — it enforces the colour+icon+text pairing.
