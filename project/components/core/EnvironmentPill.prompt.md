The global environment marker that lives in the header and on Playbook frames. Simulation is blue, Live is gold, Emergency stopped is red — never interchangeable.

```jsx
<EnvironmentPill environment="simulation" scope="Finance Genie" />
<EnvironmentPill environment="live" />
```

Exactly one of these is authoritative per surface.
