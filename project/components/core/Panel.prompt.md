Structural container with a 36px header and a scrolling body — the shell's left/right docks and any framed region.

```jsx
<Panel title="Structure" width={240} flush actions={<IconButton icon="add" label="New" size="xs" />}>…</Panel>
```

Use `flush` inside the app shell; keep the rounded form for floating panels.
