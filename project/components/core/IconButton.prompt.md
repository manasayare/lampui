Square icon-only control for toolbars, panel headers and row actions. `label` is mandatory.

```jsx
<IconButton icon="x" label="Close" size="sm" />
<IconButton icon="hand" label="Pan tool" bordered active />
```

Use only where the glyph is unambiguous; anything else gets a Button with text.
