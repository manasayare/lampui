8px-radius, 1px-border container. Flat by default — pass `raised` only when it genuinely floats.

```jsx
<Card title="Tool health" actions={<IconButton icon="ellipsis" label="More" size="sm" />}>…</Card>
<Card padding="sm" interactive selected>…</Card>
```

No coloured left borders, no shadows stacked on shadows.
