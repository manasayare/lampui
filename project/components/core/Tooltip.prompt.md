Inverse one-liner on hover/focus; `rich` gives a 240px surface for runtime detail (current task, memory source, tool health).

```jsx
<Tooltip content="Fit selection" shortcut="F"><IconButton icon="maximize" label="Fit" /></Tooltip>
```

Tooltips never carry the only copy of critical information.
