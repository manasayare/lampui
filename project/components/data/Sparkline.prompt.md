Micro trend inside metric cards, table cells, tooltips and object rows. No axes, no labels, no interaction.

```jsx
<Sparkline data={[4,6,5,9,8,12,11]} />
<Sparkline data={counts} variant="bar" color="var(--dataviz-2)" />
```

Always paired with a number — a sparkline alone is decoration.
