The structure panel: the LAMP hierarchy as a keyboard-navigable tree, with a StatusDot per object. Selection here drives the canvas and Inspector.

```jsx
<ObjectTree nodes={tree} selectedId={sel} onSelect={setSel} onToggle={toggle} expanded={open} />
```
