2–4 exclusive options: view mode, density, chart/table switch, Simulation/Live comparison.

```jsx
<SegmentedControl value={view} onChange={setView} options={[{value:'canvas',icon:'grid_view',label:'Canvas'},{value:'table',icon:'table_rows',label:'Table'}]} />
```
