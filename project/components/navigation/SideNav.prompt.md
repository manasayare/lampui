Primary navigation column: 32px rows, grouped by NavSection, gold left marker on the active item. Collapses to a 48px rail.

```jsx
<SideNav>
  <NavSection label="Operate">
    <NavItem icon="inbox" label="Approvals" meta="7" active />
    <NavItem icon="history" label="Runs" />
  </NavSection>
</SideNav>
```
