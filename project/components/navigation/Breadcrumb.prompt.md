The object path. Always reflects the real hierarchy (Business → LAMP → Genie → Playbook → Agent) and is the primary way back up.

```jsx
<Breadcrumb items={[{label:'Northwind'},{label:'Operations LAMP'},{label:'Finance Genie',icon:'hexagon'},{label:'Vendor payment'}]} />
```
