The authoritative environment strip under the header. Simulation must never look like Live; Emergency stopped must be unmistakable.

```jsx
<EnvironmentBanner environment="simulation" scope="Finance Genie"
  actions={<Button size="sm" variant="secondary" icon="stop_circle">Exit simulation</Button>} />
```

Exactly one per surface, and it always states the consequence, not just the name.
