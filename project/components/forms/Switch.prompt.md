Immediate on/off for a setting that takes effect without saving.

```jsx
<Switch label="Allow delegation" defaultChecked />
<Switch label="External writes" tone="brand" />
```

If the change needs confirmation, use a Checkbox in a form instead.
