16px checkbox, graphite when checked. Also used for table row selection.

```jsx
<Checkbox label="Require human approval above limit" description="Escalates to the Playbook owner." defaultChecked />
```
