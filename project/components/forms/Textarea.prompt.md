Multi-line text. `mono` for schemas, payloads and expressions.

```jsx
<Textarea rows={5} placeholder="Describe what this Agent is responsible for" />
```

For Agent instructions use PromptEditor instead — it supports variable tokens.
