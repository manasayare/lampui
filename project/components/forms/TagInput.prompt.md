Free-form list of short values — scopes, allowed domains, labels. Enter commits, Backspace removes the last.

```jsx
<TagInput values={scopes} onChange={setScopes} mono placeholder="Add scope" />
```
